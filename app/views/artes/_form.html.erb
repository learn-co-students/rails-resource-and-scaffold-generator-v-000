<%= form_for(@arte) do |f| %>
  <% if @arte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arte.errors.count, "error") %> prohibited this arte from being saved:</h2>

      <ul>
      <% @arte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
