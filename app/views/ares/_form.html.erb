<%= form_for(@are) do |f| %>
  <% if @are.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@are.errors.count, "error") %> prohibited this are from being saved:</h2>

      <ul>
      <% @are.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
